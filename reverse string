class Solution {
    static String conRevstr(String S1, String S2) {
        // code here
        String str=S1+S2;
        
        String ans="";
        for (int i=str.length()-1;i>=0;i--){
             char ch=str.charAt(i);
             ans=ans+ch;
        }
        return ans;
    }
}
